- name: conditions
  hosts: localhost
  become: yes
  tasks:
   - name: check user exsist or not
     ansible.builtin.command: id expense
   - name: Say hello
     ansible.builtin.debug:
      msg: "Hello"
      